import TabItem from "@theme/TabItem";
import CiTabs from "./CiTabs";

<CiTabs>
<TabItem value="azure">

```bash
npx qawolf azure
```

You'll notice that a file called `azure-pipelines.yml` was created at the root of your project. You can edit `azure-pipelines.yml.ts` to suit your needs.

</TabItem>
<TabItem value="bitbucket">

```bash
npx qawolf bitbucket
```

TODOUPDATE
You'll notice that a file called `config.yml` was created in the `.circleci` directory the root of your project. You can edit `.circleci/config.yml` to suit your needs.

</TabItem>
<TabItem value="circleci">

```bash
npx qawolf circleci
```

You'll notice that a file called `config.yml` was created in the `.circleci` directory the root of your project. You can edit `.circleci/config.yml` to suit your needs.

</TabItem>
<TabItem value="github">

```bash
npx qawolf github
```

You'll notice that a file called `qawolf.yml` was created in the `.github/workflows` directory the root of your project. You can edit `.github/workflows/qawolf.yml` to suit your needs.

</TabItem>
<TabItem value="gitlab">

```bash
npx qawolf gitlab
```

You'll notice that a file called `.gitlab-ci.yml` was created at the root of your project. You can edit `.gitlab-ci.yml` to suit your needs.

</TabItem>
<TabItem value="jenkins">

```bash
npx qawolf jenkins
```

You'll notice that a file called `Jenkinsfile` was created at the root of your project. You can edit `Jenkinsfile` to suit your needs.

</TabItem>
</CiTabs>
