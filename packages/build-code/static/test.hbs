const { launch } = require("qawolf");
const selectors = require("../selectors/{{{name}}}");

describe('{{{name}}}', () => {
  let browser;

  beforeAll(async () => {
    browser = {{{launch}}}
  });

  afterAll(() => browser.close());

{{#each steps}}
  it('can {{{it}}}', async () => {
    await {{{method}}};
  });
{{#unless @last}}

{{/unless}}
{{/each}}
});