const { launch } = require("qawolf");
const selectors = require("../selectors/{{{name}}}");

describe('{{{name}}}', () => {
  let context;

  beforeAll(async () => {
    context = {{{launch}}}
  });

  afterAll(() => context.close());

{{#each steps}}
  it('can {{{it}}}', async () => {
    await {{{method}}};
  });
{{#unless @last}}

{{/unless}}
{{/each}}
});