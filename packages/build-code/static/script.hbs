const { launch } = require("qawolf");
const selectors = require("../selectors/{{{name}}}");

const {{{name}}} = async () => {
  const context = {{{launch}}}
  {{{createSymbol}}}
  await context.close();
};

{{{name}}}();