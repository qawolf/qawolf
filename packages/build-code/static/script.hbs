const { launch } = require("qawolf");
const selectors = require("../selectors/{{{name}}}");

const {{{name}}} = async () => {
  const browser = await launch({ url: "{{{url}}}" });
{{#each steps}}
  await {{{method}}};
{{#unless @last}}
{{/unless}}
{{/each}}
  await browser.close();
};

{{{name}}}();