const { launch } = require("qawolf");
const selectors = require("../selectors/{{{name}}}");

const {{{name}}} = async () => {
  const browser = {{{launch}}}
{{#each steps}}
  // {{{it}}}
  await {{{method}}};
{{#unless @last}}
{{/unless}}
{{/each}}
  await browser.close();
};

{{{name}}}();