{
  "name": "@qawolf/config",
  "description": "qawolf config for node",
  "version": "0.3.0",
  "main": "./lib/index.js",
  "types": "./lib/index.d.ts",
  "files": [
    "lib",
    "src"
  ],
  "scripts": {
    "build": "tsc -b ./tsconfig.package.json",
    "prepublish": "npm run build"
  },
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "dotenv": "^8.1.0"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1"
  }
}
